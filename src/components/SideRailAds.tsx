/**
 * Native build of SideRailAds — the side rails are a desktop-web-only surface
 * (AdSense display units in the empty gutters beside the centered content);
 * on phones/tablets the native AdMob flows in monetization.ts are the ads.
 * See SideRailAds.web.tsx for the real implementation.
 */
export function SideRailAds() {
  return null;
}
