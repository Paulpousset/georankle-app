/**
 * Rien à faire sur natif : le survol n'existe pas au doigt. La vraie
 * implémentation est dans WebHoverStyles.web.tsx (voir lib/webHover.ts).
 */
export function WebHoverStyles() {
  return null;
}
